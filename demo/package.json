{
  "name": "react-org-chart-demo",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "dependencies": {
    "@mikhailfarberov/react-org-chart": "^1.0.1",
    "react": "^16.13.0",
    "react-dom": "^16.13.0",
    "react-scripts": "^3.4.0"
  },
  "scripts": {
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "install:clean": "rm -rf node_modules/ && rm -rf package-lock.json && npm install && npm start",
    "start": "cross-env NODE_PATH=./ npm-run-all -p start-js",
    "start-js": "react-scripts start"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "@babel/preset-react": "^7.8.3",
    "node-sass": "^4.13.1",
    "npm-run-all": "^4.1.5"
  }
}
